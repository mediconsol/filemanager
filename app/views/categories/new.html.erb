<% content_for :title, "새 카테고리 추가 - 카테고리 관리" %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "카테고리", categories_path %></li>
        <li class="breadcrumb-item active" aria-current="page">새 카테고리 추가</li>
      </ol>
    </nav>
    <h1 class="h3 mb-1">새 카테고리 추가</h1>
    <p class="text-muted mb-0">새로운 표준 필드 카테고리를 생성합니다</p>
  </div>
  <div>
    <%= link_to "취소", categories_path, class: "btn btn-outline-secondary" %>
  </div>
</div>

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header">
        <h5 class="card-title mb-0">카테고리 정보</h5>
      </div>
      <div class="card-body">
        <%= form_with model: @category, local: true do |form| %>
          <% if @category.errors.any? %>
            <div class="alert alert-danger">
              <h6>다음 오류를 수정해주세요:</h6>
              <ul class="mb-0">
                <% @category.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :name, "카테고리명", class: "form-label" %>
              <%= form.text_field :name, class: "form-control", placeholder: "예: medical_equipment" %>
              <div class="form-text">소문자, 숫자, 언더스코어만 사용 가능합니다.</div>
            </div>
            <div class="col-md-6 mb-3">
              <%= form.label :label, "표시명", class: "form-label" %>
              <%= form.text_field :label, class: "form-control", placeholder: "예: 의료장비" %>
              <div class="form-text">사용자에게 표시될 이름입니다.</div>
            </div>
          </div>

          <div class="mb-3">
            <%= form.label :description, "설명", class: "form-label" %>
            <%= form.text_area :description, class: "form-control", rows: 3,
                placeholder: "이 카테고리에 대한 설명을 입력하세요..." %>
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :sort_order, "정렬 순서", class: "form-label" %>
              <%= form.number_field :sort_order, class: "form-control", min: 0 %>
              <div class="form-text">낮은 숫자가 먼저 표시됩니다.</div>
            </div>
            <div class="col-md-6 mb-3">
              <div class="form-check mt-4">
                <%= form.check_box :is_active, class: "form-check-input", checked: true %>
                <%= form.label :is_active, "활성 상태", class: "form-check-label" %>
              </div>
            </div>
          </div>

          <div class="d-flex justify-content-between">
            <%= form.submit "카테고리 생성", class: "btn btn-primary" %>
            <%= link_to "취소", categories_path, class: "btn btn-outline-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-lg-4">
    <div class="card">
      <div class="card-header">
        <h5 class="card-title mb-0">도움말</h5>
      </div>
      <div class="card-body">
        <h6>카테고리명 규칙</h6>
        <ul class="small text-muted">
          <li>소문자로 시작</li>
          <li>영문자, 숫자, 언더스코어(_)만 사용</li>
          <li>공백이나 특수문자 사용 불가</li>
          <li>예: medical_equipment, hr_management</li>
        </ul>

        <h6>카테고리 예시</h6>
        <ul class="small text-muted">
          <li><strong>의료장비:</strong> medical_equipment</li>
          <li><strong>인사관리:</strong> hr_management</li>
          <li><strong>시설관리:</strong> facility_management</li>
          <li><strong>약품관리:</strong> pharmacy</li>
          <li><strong>보험청구:</strong> insurance_claims</li>
        </ul>

        <h6>주의사항</h6>
        <ul class="small text-muted">
          <li>카테고리명은 생성 후 변경할 수 없습니다</li>
          <li>표시명과 설명은 언제든 수정 가능합니다</li>
          <li>비활성화된 카테고리는 새 필드 생성 시 선택할 수 없습니다</li>
        </ul>
      </div>
    </div>
  </div>
</div>
