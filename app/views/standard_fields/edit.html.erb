<% content_for :title, "#{@standard_field.label} 수정 - 표준 필드 관리" %>

<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <nav aria-label="breadcrumb">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><%= link_to "표준 필드", standard_fields_path %></li>
        <li class="breadcrumb-item"><%= link_to @standard_field.label, standard_field_path(@standard_field) %></li>
        <li class="breadcrumb-item active" aria-current="page">수정</li>
      </ol>
    </nav>
    <h1 class="h3 mb-1"><%= @standard_field.label %> 수정</h1>
    <p class="text-muted mb-0">표준 필드 정보를 수정합니다</p>
  </div>
  <div>
    <%= link_to "취소", standard_field_path(@standard_field), class: "btn btn-outline-secondary" %>
  </div>
</div>

<div class="row">
  <div class="col-lg-8">
    <div class="card">
      <div class="card-header">
        <h5 class="card-title mb-0">필드 정보 수정</h5>
      </div>
      <div class="card-body">
        <%= form_with model: @standard_field, local: true do |form| %>
          <% if @standard_field.errors.any? %>
            <div class="alert alert-danger">
              <h6>다음 오류를 수정해주세요:</h6>
              <ul class="mb-0">
                <% @standard_field.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :name, "필드명", class: "form-label" %>
              <%= form.text_field :name, class: "form-control", readonly: true %>
              <div class="form-text">필드명은 변경할 수 없습니다.</div>
            </div>
            <div class="col-md-6 mb-3">
              <%= form.label :label, "표시명", class: "form-label" %>
              <%= form.text_field :label, class: "form-control" %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :category, "카테고리", class: "form-label" %>
              <%= form.select :category, StandardField.categories_for_select, {}, { class: "form-select" } %>
            </div>
            <div class="col-md-6 mb-3">
              <%= form.label :data_type, "데이터 타입", class: "form-label" %>
              <%= form.select :data_type, StandardField.data_types_for_select, {}, { class: "form-select" } %>
            </div>
          </div>

          <div class="mb-3">
            <%= form.label :description, "설명", class: "form-label" %>
            <%= form.text_area :description, class: "form-control", rows: 3 %>
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <%= form.label :sort_order, "정렬 순서", class: "form-label" %>
              <%= form.number_field :sort_order, class: "form-control", min: 0 %>
            </div>
            <div class="col-md-6 mb-3">
              <%= form.label :default_value, "기본값", class: "form-label" %>
              <%= form.text_field :default_value, class: "form-control" %>
            </div>
          </div>

          <div class="row">
            <div class="col-md-6 mb-3">
              <div class="form-check">
                <%= form.check_box :is_required, class: "form-check-input" %>
                <%= form.label :is_required, "필수 필드", class: "form-check-label" %>
              </div>
            </div>
            <div class="col-md-6 mb-3">
              <div class="form-check">
                <%= form.check_box :is_active, class: "form-check-input" %>
                <%= form.label :is_active, "활성 상태", class: "form-check-label" %>
              </div>
            </div>
          </div>

          <div class="d-flex justify-content-between">
            <%= form.submit "저장", class: "btn btn-primary" %>
            <%= link_to "취소", standard_field_path(@standard_field), class: "btn btn-outline-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-lg-4">
    <div class="card">
      <div class="card-header">
        <h5 class="card-title mb-0">현재 정보</h5>
      </div>
      <div class="card-body">
        <dl class="row small">
          <dt class="col-5">생성일:</dt>
          <dd class="col-7"><%= @standard_field.created_at.strftime('%Y-%m-%d %H:%M') %></dd>

          <dt class="col-5">수정일:</dt>
          <dd class="col-7"><%= @standard_field.updated_at.strftime('%Y-%m-%d %H:%M') %></dd>

          <dt class="col-5">현재 상태:</dt>
          <dd class="col-7">
            <% if @standard_field.is_active? %>
              <span class="badge bg-success">활성</span>
            <% else %>
              <span class="badge bg-secondary">비활성</span>
            <% end %>
          </dd>

          <dt class="col-5">필수 여부:</dt>
          <dd class="col-7">
            <% if @standard_field.is_required? %>
              <span class="badge bg-warning">필수</span>
            <% else %>
              <span class="badge bg-light text-dark">선택</span>
            <% end %>
          </dd>
        </dl>
      </div>
    </div>

    <div class="card mt-3">
      <div class="card-header bg-warning">
        <h6 class="card-title mb-0 text-dark">
          <i class="fas fa-exclamation-triangle me-2"></i>주의사항
        </h6>
      </div>
      <div class="card-body">
        <ul class="small text-muted mb-0">
          <li>필드명은 변경할 수 없습니다</li>
          <li>이미 사용 중인 필드의 데이터 타입 변경 시 주의하세요</li>
          <li>필수 필드를 비활성화하면 매핑에 영향을 줄 수 있습니다</li>
        </ul>
      </div>
    </div>
  </div>
</div>
